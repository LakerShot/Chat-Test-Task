import { JSX } from "react";
import './style.scss';

interface Props {
    messageData: any
}

export const UiChatMessage = ({ messageData }: Props): JSX.Element => {
    return (
        <div>message</div>
    )
}
